import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
const ROOT=process.cwd(),PORT=Number(process.env.ENTITY_API_PORT||4401),MEMORY_DIR=path.join(ROOT,'.entity-memories'),EMBED_DIR=path.join(ROOT,'.entity-embeddings');
const TYPES=['travail','moyen_terme','faits','histoires','graphe','modele_personne','relation','entity_core','autobiographie','monde_entity','engagements'];
const POLICY={travail:[1,3],moyen_terme:[1.08,4],faits:[1.12,5],histoires:[1.16,4],graphe:[1.1,5],modele_personne:[1.05,3],relation:[1.18,4],entity_core:[.98,2],autobiographie:[.95,3],monde_entity:[.92,3],engagements:[1.22,4]};const locks=new Map();
for(const f of ['.env','.env.local']){const p=path.join(ROOT,f);if(fs.existsSync(p))for(const l of fs.readFileSync(p,'utf8').split(/\r?\n/)){const x=l.trim(),i=x.indexOf('=');if(x&&x[0]!=='#'&&i>0&&!process.env[x.slice(0,i).trim()])process.env[x.slice(0,i).trim()]=x.slice(i+1).trim().replace(/^['"]|['"]$/g,'')}}
const safe=x=>String(x||'').replace(/[^a-zA-Z0-9_-]/g,''),read=(f,d=null)=>{try{return fs.existsSync(f)?JSON.parse(fs.readFileSync(f,'utf8')):d}catch{return d}},mf=(id,t)=>path.join(MEMORY_DIR,safe(id),`${t}.json`),sf=id=>path.join(MEMORY_DIR,safe(id),'state.json'),pf=id=>path.join(MEMORY_DIR,safe(id),'pending.json'),cf=id=>path.join(EMBED_DIR,`${safe(id)}.json`),ixf=id=>path.join(EMBED_DIR,`${safe(id)}.index.json`);
function atomic(f,d){fs.mkdirSync(path.dirname(f),{recursive:true});const t=`${f}.${process.pid}.${Date.now()}.tmp`;fs.writeFileSync(t,JSON.stringify(d,null,2));fs.renameSync(t,f)}function memory(id){const o={};let a=false;for(const t of TYPES){o[t]=read(mf(id,t));if(o[t]!=null)a=true}return a?o:null}function saveMemory(id,m){for(const t of TYPES)if(m?.[t]!=null)atomic(mf(id,t),m[t])}async function lock(id,fn){const k=String(id),p=locks.get(k)||Promise.resolve();let done;const n=new Promise(r=>done=r);locks.set(k,n);await p;try{return await fn()}finally{done();if(locks.get(k)===n)locks.delete(k)}}
function oldPrompt(n){try{const s=fs.readFileSync(path.join(ROOT,'server/entity-server.mjs'),'utf8'),m=`const ${n} = \``,a=s.indexOf(m);if(a<0)return'';const b=a+m.length,e=s.indexOf('\`;',b);return e<0?'':s.slice(b,e)}catch{return''}}const DP=oldPrompt('DIALOGUE_PROMPT'),MP=oldPrompt('MEMORY_PROMPT');
const tx=ms=>(ms||[]).map(m=>`${m.role==='assistant'?'Entity':'Personne'}: ${m.content||''}`).join('\n'),hash=s=>crypto.createHash('sha256').update(String(s)).digest('hex');
async function ai(k,input,max=1200,temp=.7){const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${k}`},body:JSON.stringify({model:process.env.ENTITY_OPENAI_MODEL||'gpt-5.4',input,max_output_tokens:max,temperature:temp})}),d=await r.json();if(!r.ok)throw Error(`OpenAI ${r.status}`);const x=d?.output?.flatMap(y=>y.content||[]).filter(y=>y.type==='output_text').map(y=>y.text).join('').trim();if(!x)throw Error('Réponse OpenAI vide');return x}async function emb(k,input){const r=await fetch('https://api.openai.com/v1/embeddings',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${k}`},body:JSON.stringify({model:process.env.ENTITY_EMBEDDING_MODEL||'text-embedding-3-small',input})}),d=await r.json();if(!r.ok)throw Error(`Embedding ${r.status}`);return d.data?.[0]?.embedding||[]}function cos(a,b){if(!a?.length||a.length!==b?.length)return 0;let d=0,x=0,y=0;for(let i=0;i<a.length;i++){d+=a[i]*b[i];x+=a[i]*a[i];y+=b[i]*b[i]}return x&&y?d/Math.sqrt(x*y):0}
function items(m){const o=[];for(const t of TYPES){const v=m?.[t];const add=(x,p)=>x&&typeof x==='object'&&o.push({type:t,item:x,path:p,text:JSON.stringify(x)});if(Array.isArray(v))v.forEach((x,i)=>add(x,`${t}.${i}`));else if(v&&typeof v==='object')for(const[k,x]of Object.entries(v))Array.isArray(x)?x.forEach((y,i)=>add(y,`${t}.${k}.${i}`)):add(x,`${t}.${k}`)}return o}function sal(x){return Math.max(0,Math.min(1,Number(x?.importance??.5)*.35+Number(x?.force_relationnelle??0)*.2+Number(x?.accessibilite??1)*.25+Math.min(10,Number(x?.nombre_confirmations??1))/10*.2))}function refs(x){const s=new Set();const w=(v,k='')=>{if(typeof v==='string'&&/(id|lien|relation|entite|personne|lieu|parent)/i.test(k))s.add(v);else if(Array.isArray(v))v.forEach(z=>w(z,k));else if(v&&typeof v==='object')for(const[a,b]of Object.entries(v))w(b,a)};w(x);if(x?.id)s.add(String(x.id));return s}function expand(seed,all){const chosen=new Set(seed.map(x=>x.path));let front=new Set(seed.flatMap(x=>[...refs(x.item)]));for(let d=0;d<2;d++){const next=new Set();for(const c of all)if(!chosen.has(c.path)){const r=refs(c.item);if([...r].some(x=>front.has(x))){chosen.add(c.path);r.forEach(x=>next.add(x))}}front=next}return all.filter(x=>chosen.has(x.path))}
async function orchestrate(id,m,conv,key,state){if(!m)return(state.working_memory||[]).map((item,i)=>({type:'working_memory',item,path:`wm.${i}`,score:1}));const all=items(m),sig=hash(all.map(x=>x.path+hash(x.text)).join('|')),cache=read(cf(id),{});let index=read(ixf(id));if(!index||index.signature!==sig){index={signature: sig,entries:{}};for(const c of all){const h=hash(c.text);if(!cache[c.path]||cache[c.path].hash!==h)cache[c.path]={hash:h,v:await emb(key,c.text)};index.entries[c.path]=h}for(const p of Object.keys(cache))if(!index.entries[p])delete cache[p];atomic(cf(id),cache);atomic(ixf(id),index)}const q=await emb(key,conv),scored=all.map(c=>({...c,score:cos(q,cache[c.path]?.v)*.66+sal(c.item)*.25+(POLICY[c.type]?.[0]||1)*.09})).sort((a,b)=>b.score-a.score),counts={};let sel=[];const budget=Math.max(7,Math.min(18,8+Math.round(conv.length/700)));for(const c of scored){const max=POLICY[c.type]?.[1]||3;if((counts[c.type]||0)>=max)continue;sel.push(c);counts[c.type]=(counts[c.type]||0)+1;if(sel.length>=budget)break}sel=expand(sel,all);for(const t of ['entity_core','engagements'])if(!sel.some(x=>x.type===t)){const c=all.find(x=>x.type===t);if(c)sel.push(c)}const wm=(state.working_memory||[]).slice(-8).map((item,i)=>({type:'working_memory',item,path:`wm.${i}`,score:1}));return[...wm,...sel].slice(0,24)}
function merge(a,b){if(Array.isArray(a)||Array.isArray(b)){const o=Array.isArray(a)?structuredClone(a):[];for(const n of Array.isArray(b)?b:[]){const i=n?.id?o.findIndex(x=>String(x?.id)===String(n.id)):-1;i<0?o.push(n):o[i]=merge(o[i],n)}return o}if(a&&typeof a==='object'&&b&&typeof b==='object'){const o={...a};for(const k in b)o[k]=k in o?merge(o[k],b[k]):b[k];return o}return b}const clean=s=>String(s).replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'').trim();
async function consolidate(id,m,conv,key,state){const rel=await orchestrate(id,m,conv,key,state),fr={};for(const x of rel)if(x.type!=='working_memory')(fr[x.type]??=[]).push(x.item);const p=`${MP}\n\nCONSOLIDATION SÉLECTIVE: renvoie seulement un objet JSON avec les catégories modifiées. Une correction explicite remplace l'ancien fait. Une contradiction non résolue reste marquée à vérifier. Ne reconstruis pas toute la mémoire.\nEXISTANT PERTINENT:${JSON.stringify(fr)}\nNOUVEAUX ÉCHANGES:${conv}`;let patch;for(let i=0;i<2&&!patch;i++)try{patch=JSON.parse(clean(await ai(key,p,1800,.15)))}catch{}if(!patch)return m;const o={...(m||{})};for(const t of TYPES)if(patch[t]!==undefined)o[t]=merge(o[t],patch[t]);return o}
function dep(x){const s=String(x||'');return/^(j['’]?y vais|je file|à plus|salut|bonne soirée|bonne nuit|bye|ciao)/i.test(s)?'effective':'aucune'}function valid(x,d){if(!x)return false;if(d==='aucune'&&/(à la prochaine|au revoir|bonne fin de journée|bonne soirée|à bientôt)/i.test(x))return false;return x.length<5000}async function generate(id,conv,m,d,key,state){const rel=await orchestrate(id,m,conv,key,state),p=`${DP}\n\nMÉMOIRE ORCHESTRÉE:${JSON.stringify(rel)}\n\nCONVERSATION:${conv}\nNe présente jamais une hypothèse comme un fait. Ne résous jamais arbitrairement deux souvenirs contradictoires.`;for(let i=0;i<2;i++){const x=(await ai(key,p,1200,.7)).trim();if(valid(x,d))return x}throw Error('Réponse Entity invalide')}
async function handle(b){const key=process.env.OPENAI_API_KEY;if(!key)throw Error('OPENAI_API_KEY manquante');if(!b.entityId||!Array.isArray(b.messages)||!b.messages.length)throw Error('Requête Entity invalide');const id=b.entityId,state=read(sf(id),{schema_version:4,revision:0,user_turns:0,working_memory:[]}),m=memory(id),recent=b.messages.slice(-12),conv=tx(recent),latest=b.messages.filter(x=>x.role==='user').at(-1)?.content||'',start=Date.now(),message=await generate(id,conv,m,dep(latest),key,state);const pending=read(pf(id),[]);pending.push({role:'user',content:latest},{role:'assistant',content:message});atomic(pf(id),pending);state.working_memory=[...(state.working_memory||[]),{at:new Date().toISOString(),user:latest,entity:message}].slice(-8);state.user_turns=Number(state.user_turns||0)+1;state.revision=Number(state.revision||0)+1;state.updated_at=new Date().toISOString();if(state.user_turns%6===0){const merged=await consolidate(id,m,tx(pending),key,state);if(merged){saveMemory(id,merged);if(fs.existsSync(ixf(id)))fs.unlinkSync(ixf(id));atomic(pf(id),[])}}atomic(sf(id),state);return{message,meta:{memory_engine:'v4',schema_version:4,revision:state.revision,request_ms:Date.now()-start}}}
const server=http.createServer(async(req,res)=>{try{if(req.method==='POST'&&req.url==='/api/entity'){const b=await new Promise((ok,no)=>{let s='';req.on('data',c=>s+=c);req.on('end',()=>{try{ok(JSON.parse(s||'{}'))}catch{no(Error('JSON invalide'))}});req.on('error',no)}),r=await lock(b.entityId,()=>handle(b));res.writeHead(200,{'Content-Type':'application/json'});return res.end(JSON.stringify(r))}if(req.method==='GET'&&req.url==='/health'){res.writeHead(200,{'Content-Type':'application/json'});return res.end(JSON.stringify({ok:true,memory_engine:'v4'}))}res.writeHead(404);res.end()}catch(e){console.error('[entity]',e.message);if(!res.headersSent)res.writeHead(500,{'Content-Type':'application/json'});res.end(JSON.stringify({error:e.message}))}});server.listen(PORT,'127.0.0.1',()=>console.log(`[entity] API locale sur http://127.0.0.1:${PORT}`));