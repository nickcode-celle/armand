FROM node:22-bookworm-slim
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY api ./api

ENV NODE_ENV=production
ENV HOST=0.0.0.0
ENV PORT=4401
ENV ENTITY_STORAGE_DIR=/data

VOLUME ["/data"]
EXPOSE 4401

CMD ["node","server/entity-server-v2.mjs"]
