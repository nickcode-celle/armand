import assert from 'node:assert/strict';import {ENTITY_SCHEMA_VERSION,ENTITY_MIGRATIONS,migrateEntityState} from '../server/entity-schema.mjs';
assert.equal(ENTITY_SCHEMA_VERSION,8);assert.deepEqual(ENTITY_MIGRATIONS,[6,7,8]);const migrated=migrateEntityState({schema_version:5,revision:12});assert.equal(migrated.schema_version,8);assert.equal(migrated.runtime_version,1);assert.equal(migrated.recall_version,2);assert.equal(migrated.metrics.consolidation_failures,0);let future=false;try{migrateEntityState({schema_version:99})}catch{future=true}assert.equal(future,true);console.log('Entity schema migration runtime tests: OK');
